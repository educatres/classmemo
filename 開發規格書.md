# classMemo 教室便條貼｜開發規格書

## 1. 專案目的

classMemo 是供課堂匿名共創使用的即時便條貼白板。它以 Firebase Realtime Database 取代 Google Form／Google Sheet 的中繼資料流程，降低多人同時操作時的同步延遲。

- 正式網站：<https://educatres.github.io/classmemo/>
- GitHub 儲存庫：<https://github.com/educatres/classmemo>
- Firebase 專案 ID：`classmemo`
- Realtime Database：`classmemo-default-rtdb`（`asia-southeast1`）

## 2. 使用者與權限

| 角色 | 進入方式 | 可執行操作 |
| --- | --- | --- |
| 未登入訪客 | 只有白板 ID | 無法讀取白板內容；頁面停留在登入閘門。 |
| 學生 | 三位數學生密鑰或含 `student_key` 的分享網址 | 未凍結且未過期時，可新增、編輯、拖曳、縮放、換色與刪除便條貼。 |
| 老師 | 六位數老師密鑰 | 擁有學生權限，以及凍結、清除、刪除、匯入／下載 JSON、產生 QR Code、匯出 PNG／SVG 等管理功能。 |

老師密鑰不是個人帳號密碼；任何人取得該白板連結與正確六位數密鑰，都能成為該白板的老師。請勿將老師密鑰提供給學生。

## 3. 功能規格

### 3.1 首頁（`index.html`）

1. 建立白板時，產生：
   - 白板 ID：`b` 加九位數字、每三位以連字號分組，例如 `b000-000-000`。
   - 老師六位數密鑰。
   - 學生三位數密鑰。
2. 顯示含 `board_id` 與 `student_key` 的學生分享網址及 QR Code。
3. 「本瀏覽器開啟過的白板」清單讀取 LocalStorage，提供再次開啟與距離失效時間。
4. 「清除本機記錄」只刪除本瀏覽器的 LocalStorage 記錄，不會刪除 Firebase 白板。
5. 「目前所有白板」預設隱藏，透過右下角半透明 `▤` 圖示才可開啟；其連結僅帶 `board_id`，不公開學生密鑰。
6. 右下角 `🔑` 圖示可顯示白板名稱與 Firebase 路徑，供維護查核使用。

### 3.2 白板（`board.html`）

1. 進入時先顯示全畫面登入閘門，背景白板模糊且不可操作。
2. 含學生密鑰的網址會自動 claim 學生權限；也可手動輸入三位數學生密鑰。
3. 登入閘門可切換成老師六位數密鑰登入。
4. 便條貼可新增、儲存編輯內容、刪除、拖曳、縮放與換色；外觀為 5px 圓角。
5. 透過 Firebase `onValue` 即時監聽同步，另每 10 秒主動讀取一次作為補強；同步狀態顯示在可隱藏 UI 的右上角浮水印。
6. 老師工作區：
   - 顯示／複製老師密鑰，並顯示學生三位數密鑰。
   - 顯示學生分享網址與 QR Code。
   - 凍結或解除學生編輯權限。
   - 清除所有便條貼、刪除整張白板。
   - 匯入 JSON 回復便條貼；下載目前白板 JSON。
   - 將整個無邊際白板匯出為 PNG 或 SVG。
7. 白板建立後有效 3 天，頁面持續顯示距離失效時間。

## 4. 本機記錄規格

`js/local-boards.js` 使用 LocalStorage key `classMemo.openedBoards.v1` 保存本機曾成功進入的白板：

```json
[
  {
    "boardId": "b000-000-000",
    "studentKey": "123",
    "teacherKey": "123456",
    "createdAt": 0,
    "lastOpenedAt": 0
  }
]
```

- 重新整理白板時，優先使用網址中的 `student_key`，否則使用本機保存的學生密鑰。
- Firebase Anonymous Authentication 亦會在同一瀏覽器延續匿名 UID，因此老師登入後重新整理仍可保有老師授權。
- 此資料只存在目前瀏覽器；共用電腦使用完畢應按首頁「清除本機記錄」。

## 5. Firebase 資料結構

```text
boards/{boardId}/
  settings/
    frozen: boolean
    created_at: timestamp(ms)
  admins/{uid}: true
  notes/{noteId}/
    note_id, text, x, y, width, height, color, z_index, updated_at

boardCatalog/{boardId}/
  board_id, created_at

teacherKeys/{boardId}: "123456"
teacherKeyClaims/{boardId}/{uid}: "123456"
studentKeys/{boardId}: "123"
studentKeyClaims/{boardId}/{uid}: "123"
```

`boardCatalog` 用於首頁清單；老師與學生密鑰本身分開儲存，只有符合規則的 UID 可寫入相對應的 claim。便條貼欄位由資料庫規則驗證，文字上限 1,000 字、寬度 120–600、高度 80–400。

## 6. 安全與到期規則

規則檔為 [`database.rules.json`](./database.rules.json)。核心約束如下：

1. 所有讀寫都必須先完成 Firebase 匿名登入。
2. `notes` 的讀取須為老師，或具有與 `studentKeys` 相符的學生 claim。
3. 學生寫入還必須具備正確學生 claim、白板未凍結且未過期。
4. 老師 claim 須與六位數 `teacherKeys` 相符，才可寫入 `admins/{uid}`。
5. 到期時間是 `created_at + 259200000`（72 小時）；過期後一般寫入被拒絕，且首頁／白板程式會嘗試清除過期資料。

Firebase Console 必須啟用 **Authentication → Anonymous**，並在授權網域加入：

- `educatres.github.io`
- `localhost`
- `127.0.0.1`

## 7. 程式架構

| 路徑 | 職責 |
| --- | --- |
| `index.html` / `js/index.js` | 建立白板、分享資訊、首頁清單、本機白板清單。 |
| `board.html` / `js/board.js` | 登入閘門、便條貼互動、即時同步、老師控制台、匯入與匯出。 |
| `js/firebase-store.js` | Firebase App、匿名登入、Realtime Database 存取函式。 |
| `js/local-boards.js` | LocalStorage 讀寫與本機記錄清除。 |
| `js/config.js` | 網址參數解析、分享網址建立、白板 ID 產生。 |
| `js/board-pan.js` | 無邊際白板平移。 |
| `js/ui-visibility.js` | 工作區 UI 隱藏／顯示。 |
| `js/qr.js` | QR Code 產生。 |
| `css/styles.css` | 響應式版面、便條貼、登入閘門與老師控制台樣式。 |
| `database.rules.json` | Realtime Database 安全規則。 |

## 8. 開發、測試與部署

### 本機預覽

```bash
python3 -m http.server 8080
```

開啟 <http://localhost:8080/>。請以 HTTP 伺服器預覽，不要直接開啟 HTML 檔案，避免 ES module 與 Firebase 載入限制。

### 基本檢查

```bash
node --check js/index.js
node --check js/board.js
node --check js/firebase-store.js
node --check js/local-boards.js
node -e "JSON.parse(require('fs').readFileSync('database.rules.json', 'utf8'))"
git diff --check
```

### 部署 Firebase 規則

```bash
firebase deploy --only database --project classmemo
```

### 部署網站

```bash
git add .
git commit -m "說明本次變更"
git push origin main
```

GitHub Pages 由 `main` 分支根目錄發布。推送後可查核：

```bash
gh run list --repo educatres/classmemo --limit 1
```

## 9. 維護注意事項

- Firebase Web App 設定位於 `js/firebase-config.js`；它是 Web 用公開設定，不可在此放入服務帳號金鑰或其他私密憑證。
- 修改資料結構時，必須同步更新 `database.rules.json`、前端驗證與本規格書。
- 不可移除學生密鑰存在檢查；僅比較兩個可能同時為空的值，可能導致未授權存取。
- 白板 ID 含連字號，符合 Firebase Realtime Database key 規則；仍不得使用 `. # $ [ ] /` 等保留字元。
